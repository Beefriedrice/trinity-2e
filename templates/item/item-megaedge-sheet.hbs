<form class='{{cssClass}}' autocomplete='off'>
  <header class='sheet-header'>
    <img
      class='profile-img'
      src='{{item.img}}'
      data-edit='img'
      title='{{item.name}}'
    />
    <div class='header-fields'>
      <h1 class='charname'><input
          name='name'
          type='text'
          value='{{item.name}}'
          placeholder='Name'
        /></h1>
      <div class='grid grid-2col'>
        <div class='resource'>
          <label class='resource-label'>{{localize "TC.Labels.Rating"}}</label>
          <input
            type='text'
            class="text-2digit"
            name='system.rating'
            value='{{system.rating}}'
            data-dtype='Number'
          />
        </div>
        <div class='resource'>
          <label class='resource-label'>{{localize "TC.Power.Maxed"}}</label>
          {{#if system.maxedout}}
            <input type="checkbox" class="medium-check" name="system.maxedout" value="true" checked data-dtype="Boolean"/>
          {{else}}
            <input type="checkbox" class="medium-check" name="system.maxedout" value="false" data-dtype="Boolean"/>
          {{/if}}
        </div>
            
        <div class='resource-text grid-span-2'>
        <label class="resource-label">{{localize "TC.Labels.Prereq"}}</label><br>
        <input
          type='text'
          name='system.prereq'
          value='{{system.prereq}}'
          data-dtype='String'
        />
      </div>
      </div>
    </div>
  </header>

  {{! Sheet Tab Navigation }}
  <nav class='sheet-tabs tabs' data-group='primary'>
    <a class='item' data-tab='description'>{{localize "TC.Labels.Description"}}</a>
    <a class='item' data-tab='tags'>{{localize "TC.Labels.Tags"}}</a>
    <a class='item' data-tab='attributes'>{{localize "TC.Labels.Attributes"}}</a>
  </nav>

  {{! Sheet Body }}
  <section class='sheet-body'>

    {{! Description Tab }}
    <div class='tab' data-group='primary' data-tab='description'>
    {{! Editors must receive enriched text data from getData to properly handle rolls }}
    {{editor
      enrichedDescription
      target='system.description'
      engine='prosemirror'
      button=true
      editable=editable
    }}
    </div>

    {{! Tags Tab }}
    <div class='tab' data-tab='tags'>  
      <h3>{{localize "TC.Labels.Tags"}}</h3>
      <div class="resource-text">
        <input
          type='text'
          name='system.tags'
          value='{{system.tags}}'
          data-dtype='String'
        />
      </div>
      <h3 class="description added-segment">{{localize "TC.Power.TagD"}}</h3>
        {{! Editors must receive enriched text data from getData to properly handle rolls }}
        {{editor
          enrichedDescription
          target='system.tagdescription'
          engine='prosemirror'
          button=true
          editable=editable
        }}
    </div>

    {{! Attributes Tab }}
    <div class='tab attributes' data-group='primary' data-tab='attributes'>
      {{! As you add new fields, add them in here! }}
      {{> "systems/trinity-continuum/templates/item/parts/item-attributes.hbs"}}
    </div>
  </section>
</form>