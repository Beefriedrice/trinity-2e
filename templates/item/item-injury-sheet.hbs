<form class='{{cssClass}}' autocomplete='off'>
  <header class='sheet-header'>
    <img
      class='profile-img'
      src='{{item.img}}'
      data-edit='img'
      title='{{item.name}}'
    />
    <div class='header-fields'>
      <h1 class='charname'><input
          name='name'
          type='text'
          value='{{item.name}}'
          placeholder='Name'
        /></h1>
      <div class='grid grid-2col'>
        <div class='resource-text'>
          <label class='resource-label'>{{localize "TC.SheetLabels.Wound"}}</label>
          <input
            type='text'
            name='system.wound'
            value='{{system.wound}}'
            list='wound-type'
            data-dtype='String'
          />
          <datalist id="wound-type">
            <option value="Armor">{{localize "TC.SheetLabels.Armor"}}</option>
            <option value="Bruised">{{localize "TC.SheetLabels.Bruised"}}</option>
            <option value="Injured">{{localize "TC.SheetLabels.Injured"}}</option>
            <option value="Maimed">{{localize "TC.SheetLabels.Maimed"}}</option>
          </datalist>
        </div>
        <div class='resource-text'>
          <label class='resource-label'>{{localize "TC.SheetLabels.Woundsource"}}</label>
          <input
            type='text'
            name='system.source'
            value='{{system.source}}'
            data-dtype='String'
          />
        </div>
        <div class='resource'>
          <label class='resource-label'>{{localize "TC.SheetLabels.Firstaid"}}</label>
          {{#if system.firstaid}}
            <input type="checkbox" class="medium-check" name="system.firstaid" value="true" checked data-dtype="Boolean"/>
          {{else}}
            <input type="checkbox" class="medium-check" name="system.firstaid" value="false" data-dtype="Boolean"/>
          {{/if}}
        </div>
      </div>
    </div>
  </header>

  {{! Sheet Tab Navigation }}
  <nav class='sheet-tabs tabs' data-group='primary'>
    <a class='item' data-tab='description'>{{localize "TC.SheetLabels.Description"}}</a>
    <a class='item' data-tab='attributes'>{{localize "TC.SheetLabels.Attributes"}}</a>
  </nav>

  {{! Sheet Body }}
  <section class='sheet-body'>

    {{! Description Tab }}
    <div class='tab' data-group='primary' data-tab='description'>
      <h3 class="added-segment">{{localize "TC.SheetLabels.Effect"}}</h3>
      {{! Editors must receive enriched text data from getData to properly handle rolls }}
      {{editor
        enrichedDescription
        target='system.contact.effect'
        engine='prosemirror'
        button=true
        editable=editable
      }}
    </div>

    {{! Attributes Tab }}
    <div class='tab attributes' data-group='primary' data-tab='attributes'>
      {{! As you add new fields, add them in here! }}
      {{> "systems/trinity-continuum/templates/item/parts/item-attributes.hbs"}}
    </div>
  </section>
</form>